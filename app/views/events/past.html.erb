<h1>Past Events</h1>

<ul>
  <% @events.each do |event| %>
  <li><%= link_to event.name, event_path(event) %></li>
  <% end %>
</ul>

<%= link_to "Back to All Events", events_path, class: "button index" %><br>