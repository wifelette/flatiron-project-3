<h1>Current and Future Events</h1>

<ul>
  <% @events.each do |event| %>
  <li><%= link_to event.name, event_path(event) %></li>
  <% end %>
</ul>

<%= link_to "Back to All Events", events_path, class: "btn btn-primary" %><br>